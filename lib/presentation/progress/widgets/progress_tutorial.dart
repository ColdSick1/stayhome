import 'package:flutter/material.dart';

class ProgressTutorial extends StatelessWidget {
  const ProgressTutorial({super.key});

  @override
  Widget build(BuildContext context) {
    return Column(
      mainAxisAlignment: MainAxisAlignment.start,
      children: [
        Text('data'),
        Text('data'),
        Text('data'),
      ],
    );
  }
}
